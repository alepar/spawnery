/**
 * Proof-of-Possession (PoP) construction for /refresh [AM5].
 *
 * Sourced from @spawnery/client — see sdk/ts/src/signing/pop.ts for implementation notes.
 */
export { buildPoP, POP_DOMAIN, type PoPHeaders } from "@spawnery/client";
